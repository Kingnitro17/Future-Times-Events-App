import 'package:flutter_test/flutter_test.dart';

import 'package:future_times_events/main.dart';

void main() {
  test('the application root widget is available', () {
    expect(FutureTimesApp, isA<Type>());
  });
}