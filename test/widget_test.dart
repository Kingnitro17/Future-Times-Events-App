import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Full integration tests require Supabase public configuration.
    expect(true, isTrue);
  });
}
